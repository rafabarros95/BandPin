[project]
name = "bandpin"
version = "0.1.0"
description = "BandPin for Pin Authentication"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "esptool>=5.2.0",
    "mpremote>=1.28.0",
]
